{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "connect-dialog",
  "description": "Capability for the connect window",
  "windows": ["connect-dialog"],
  "permissions": ["core:default", "core:window:allow-close", "dialog:default", "store:default"]
}
